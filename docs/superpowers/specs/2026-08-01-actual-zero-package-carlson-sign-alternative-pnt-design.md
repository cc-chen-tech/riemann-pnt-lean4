# Actual Zero-Package Carlson Sign-Alternative PNT Transfer

## Objective

Instantiate the stack93 finite-seed theorem with the actual equal-real-part
zeta-zero package and the stack92 natural-point sign alternative.

## Inputs

The theorem keeps every analytic condition explicit:

- `1/2 < sigma < 1` and `(1 + sigma) / 2 < beta`;
- a positive logarithmic window and positive package energy;
- `0 < q < 1` for the sampled mean-square coefficient;
- an outside-cluster real-part cap and strict real-ordinate inequality;
- Carlson outside-boundary mass below
  `q * sqrt(actualEqualRealPartZeroPackageEnergy T beta L) / 2`.

## Construction

Choose the balanced selected-good-height schedule. Prove its cofinality from
the positive balanced exponent. Discharge conjugation invariance using the
existing actual-package theorem and discharge target-line seed membership via
`mem_equalRealPartZeroPackage`.

Apply the stack92 natural-point sign alternative at exact coefficient
`q * sqrt(energy)`. Feed that disjunction into stack93, which performs finite
target-line capture and one-sided PNT transfer.

## Output and claim boundary

The output contains fixed-rate relative PNT convergence, the finite extension
and boundary budget, and an actual relative PNT witness of one persistent sign
at coefficient `(q * sqrt(energy) - loss) / 2` times `x^(beta - 1)`.

This is conditional `Omega+ OR Omega-`. It is not simultaneous `Omega+-`, RH,
or a new local pi/2 theorem. Carlson cap and boundary hypotheses remain
external and visible.
