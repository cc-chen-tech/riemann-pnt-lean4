# Canonical Polynomial Unified Upper and Signed Transfer

## Objective

Instantiate the complete variable-boundary explicit-formula transfer with the
canonical polynomial-height window from Stack141.

## Automatically discharged inputs

- positive inner and outer polynomial exponents;
- selected-height domination by the outer polynomial height;
- selected-height cofinality;
- contour target-amplitude margin;
- positive logarithmic slack;
- strict Carlson residual exponent margin;
- actual multiplicity-aware explicit-formula remainder certificate;
- the uniform Carlson coefficient cap used by the upper bound.

## Remaining mathematical inputs

- moving indexed right-edge geometry;
- exclusion of real-ordinate zeros to the right of `beta0`;
- positive and negative natural-point witnesses for the visible main package.

These are genuine zero geometry and anti-cancellation statements rather than
height-selection bookkeeping.

## Claim boundary

The theorem is conditional on both signed main-package witnesses. It does not
construct those witnesses and therefore does not assert an unconditional
Omega theorem or RH.
