# Optimal Polynomial Sigma-Only Unified Transfer

## Objective

Install Stack145's minimax-optimal quarter-balanced height parameters in the
complete sigma-only running-boundary transfer.

## Automatic chain

```text
sigma
  -> optimized critical-half/real-zero beta0
  -> G = 2 beta0 - 1 - sigma
  -> optimal common margin G/4
  -> inner selected height and outer Carlson height
  -> actual explicit-formula remainder
  -> natural running visible-zero boundary
  -> Carlson coefficient cap and normalized complement
  -> matching PNT upper bound and conditional signed transfer
```

The public theorem retains only positive and negative witnesses for the
concrete optimal visible-main function.

## Claim boundary

The height allocation is minimax optimal within the four-margin model. The
signed witnesses are not constructed, so no unconditional Omega theorem or RH
is claimed.
