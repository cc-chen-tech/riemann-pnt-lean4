# Actual zero-package reciprocal sign alternative design

The actual equal-real-part zeta-zero package supplies an unsigned natural-point
mean-square witness with coefficient `c = q * sqrt(E)`.  Infinite two-color
pigeonhole converts it to a persistent positive or persistent negative main
witness.

For outside Carlson boundary mass `B < c/2`, choose the surviving coefficient

```text
ell = (c - 2 * B) / 2 > 0.
```

Stack160 bounds the actual explicit-formula residual by
`(2 * B + ell) * x^(beta-1)`, so the transferred coefficient is exactly `ell`.

The selected height uses the near-optimal contour window.  The reciprocal low
layer requires only `sigma < beta`, replacing the older balanced threshold
`(1+sigma)/2 < beta`.

The conclusion is a sign alternative, not simultaneous positive and negative
witnesses.  It does not claim Omega-plus/minus or RH.
