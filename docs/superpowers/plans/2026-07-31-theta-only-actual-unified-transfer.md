# Theta-Only Actual Unified Transfer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Run the actual globally optimized transfer with theta as the only numerical input.

**Architecture:** Compose stack69's canonical strict target exponent with stack63's improved-cap automatic transfer. Return the boundary exponent and every automatically selected truncation parameter for auditability.

**Tech Stack:** Lean 4, inverse improved threshold, global sigma optimizer, actual selected-good-height explicit formula.

## Global Constraints

- Modify only `ZeroDensityLayerBudget*.lean` and task design/plan files.
- Do not modify `ZeroForcedOscillationComplementaryBound.lean`.
- Do not modify VK-edge or sharp-oscillation files.
- Keep the visible-cluster witness explicit.

---

### Task 1: Theta-only actual transfer

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualThetaOnlyUnifiedTransfer.lean`

**Interfaces:**
- Consumes: canonical strict target exponent and improved-cap automatic transfer.
- Produces: `exists_thetaOnlyAutomaticGoodHeight_globalRealPartBoundNaturalTargetTransfer`.

- [ ] **Step 1: select boundary and strict target exponents**

- [ ] **Step 2: invoke the improved-cap automatic transfer**

- [ ] **Step 3: return all selected parameters and defining identities**

### Task 2: Contract, audit, and publication

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualThetaOnlyUnifiedTransferContract.lean`
- Create: `Test/ZeroDensityLayerBudgetActualThetaOnlyUnifiedTransferAxiomAudit.lean`

- [ ] **Step 1: compile implementation and contract directly**

- [ ] **Step 2: run the focused axiom audit**

- [ ] **Step 3: commit docs and code separately**

- [ ] **Step 4: push and create a draft PR based on stack69**
