# Real-window interpolation plan

1. Prove the real jump is nonnegative and at most `log x` for `x >= 1`.
2. Expose the exact floor identities for `psi` and `psi0`.
3. Bound the midpoint discrepancy by the two logarithms.
4. Combine the floor discrepancy with fixed-height spatial variation and the
   common natural-point estimate.
5. Verify and submit the first complete real-window endpoint.
