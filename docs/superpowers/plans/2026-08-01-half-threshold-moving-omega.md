# Stack170 half-threshold moving Omega plan

1. Replace the two-height moving-complement input in the moving-seed lower
   transfer by Stack168 reciprocal negligibility.
2. Choose canonical `sigma`, `tau`, and contour exponent `alpha` from
   `1/2 < beta < 1`.
3. Select an actual equal-real-part package with positive energy and small
   Carlson boundary mass.
4. Use Stack169 reciprocal absolute mass to control every moving extension.
5. Transfer the package mean-square witness through the dynamic cluster and
   the actual explicit formula.
6. Convert to unnormalized `x^beta` scale, instantiate the attained-edge
   real-closed seed, compile, audit, and publish a chained draft PR.
