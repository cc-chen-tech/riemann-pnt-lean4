# Stack167 actual zero-package moving-right-edge Omega plan

1. Select the two-height moving Carlson geometry above the `2/3` threshold.
2. Select an actual equal-real-part package with positive energy and boundary
   mass less than half its sampled coefficient.
3. Use the package mean-square theorem as the fixed finite seed witness.
4. Bound the moving-cluster extension by twice the residual boundary mass
   plus half of the remaining coefficient gap.
5. Apply the moving-seed explicit-formula transfer and convert the relative
   witness to the unnormalized `x^beta` scale.
6. Instantiate the general theorem with the attained right-edge real-closed
   seed, then compile the source, contract, and axiom audit.
