# Boundary-captured signed PNT transfer plan

1. Reuse automatic joint two-height parameter selection above `beta > 2/3`.
2. Select a finite target-line Carlson cluster with boundary gap `c - q`.
3. Prove the selected good-height full outside absolute-mass estimate.
4. Convert the normalized moving-extension bound into coefficient loss
   `(c - q)`.
5. Feed captured-cluster positive and negative witnesses into the fixed signed
   moving-seed transfer.
6. Expose PNT convergence and signed coefficient `q / 2` through a contract and
   axiom audit.

