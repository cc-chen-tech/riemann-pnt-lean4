# Automatic Reverse Cluster Exclusion Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Derive empty visible cluster from target-scale PNT negligibility through the automatic global-real-part-bound transfer.

**Architecture:** Add a generic real-domain witness incompatibility lemma, then compose it with stack48's actual-error far-witness conclusion.

**Tech Stack:** Lean 4, filter limits, actual zeta-zero clusters, automatic good-height explicit formula.

## Global Constraints

- Modify only `ZeroDensityLayerBudget*.lean` and task design/plan files.
- Do not modify `ZeroForcedOscillationComplementaryBound.lean`.
- Do not modify VK-edge or sharp-oscillation files.
- Keep the visible-cluster witness as an explicit conditional input.

---

### Task 1: Real-domain witness incompatibility

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualAutomaticReverseClusterExclusion.lean`

**Interfaces:**
- Consumes: `TargetAmplitudeNegligible`, eventual amplitude positivity, and `HasFarTargetAmplitudeWitness`.
- Produces: `TargetAmplitudeNegligible.not_hasFar_mul`.

- [ ] **Step 1: Obtain an eventual strict `c * amplitude` bound**

- [ ] **Step 2: Evaluate a far witness beyond that threshold**

- [ ] **Step 3: Derive the strict-order contradiction**

### Task 2: Automatic reverse cluster exclusion

**Files:**
- Modify: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualAutomaticReverseClusterExclusion.lean`

**Interfaces:**
- Consumes: `exists_automaticGoodHeight_globalRealPartBoundNaturalTargetTransfer`.
- Produces: `exists_automaticGoodHeight_globalRealPartBound_reverseClusterExclusion`.

- [ ] **Step 1: Obtain automatic `sigma`, `tau`, and `alpha`**

- [ ] **Step 2: Assume the enlarged cluster is nonempty**

- [ ] **Step 3: Transfer its conditional main witness to the actual error**

- [ ] **Step 4: Contradict target-scale PNT negligibility**

### Task 3: Contract, audit, and publication

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualAutomaticReverseClusterExclusionContract.lean`
- Create: `Test/ZeroDensityLayerBudgetActualAutomaticReverseClusterExclusionAxiomAudit.lean`

**Interfaces:**
- Consumes: both new theorems.
- Produces: focused API and axiom evidence.

- [ ] **Step 1: Add `#check` contract entries**

- [ ] **Step 2: Add `#print axioms` entries**

- [ ] **Step 3: Compile implementation, contract, and audit directly**

- [ ] **Step 4: Commit docs and code separately**

- [ ] **Step 5: Open a draft PR based on stack48**
