# Explicit-formula uniform-window truncation plan

1. Lock the exact spatial-variation signatures in a contract.
2. Prove the one-zero Lipschitz estimate by the real mean-value theorem.
3. Sum the estimate with analytic multiplicity over
   `nontrivialZerosFinset T`.
4. Add focused and central axiom audits.
5. Run the focused build, exact contract, axiom allowlist, baseline
   verification, and full build.
6. Only after this milestone passes, assemble the natural-point estimate with
   floor/ceiling arithmetic and midpoint-jump control.
