# Actual Zero-Package Carlson Sign-Alternative PNT Transfer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produce a direct actual-zeta-package theorem yielding one persistent sign in the relative PNT error through the stack92/93 transfer chain.

**Architecture:** A single facade proves package structural facts, constructs the balanced selected-height schedule, imports the package sign disjunction, and invokes the generic half-threshold seed transfer.

**Tech Stack:** Lean 4, Mathlib filters, actual zeta-zero package energy, Carlson boundary budgets, explicit-formula PNT transfer.

## Global Constraints

- Modify only the new `ZeroDensityLayerBudgetActualZeroPackageCarlsonSignAlternativePNTTransfer*` files, matching audit, and docs.
- Keep all Carlson cap and real-ordinate conditions explicit.
- Claim only conditional `Omega+ OR Omega-`; do not claim simultaneous signs or RH.

---

### Task 1: Actual package facade

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualZeroPackageCarlsonSignAlternativePNTTransfer.lean`

**Interfaces:**
- Consumes: `equalRealPartZeroPackage_isConjugationInvariant`, `mem_equalRealPartZeroPackage`, stack92 package sign alternative, and stack93 half-threshold sign transfer.
- Produces: `exists_actualZeroPackage_actualCarlsonHalfThresholdPNTSignAlternative`.

- [ ] **Step 1:** Construct the balanced selected-height schedule and prove cofinality.
- [ ] **Step 2:** Derive package conjugation invariance and target-line seed membership.
- [ ] **Step 3:** obtain the exact `q * sqrt(energy)` sign disjunction.
- [ ] **Step 4:** invoke stack93 and preserve the net half-threshold coefficient.

### Task 2: Contract and audit

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualZeroPackageCarlsonSignAlternativePNTTransferContract.lean`
- Create: `Test/ZeroDensityLayerBudgetActualZeroPackageCarlsonSignAlternativePNTTransferAxiomAudit.lean`

**Interfaces:**
- Consumes: the Task 1 facade.
- Produces: a public type check and axiom report.

- [ ] **Step 1:** compile the main facade directly with the local olean overlay.
- [ ] **Step 2:** compile the contract.
- [ ] **Step 3:** compile the axiom audit and inspect its dependency list.
- [ ] **Step 4:** commit docs separately from Lean code and publish a bounded draft PR based on stack93.
