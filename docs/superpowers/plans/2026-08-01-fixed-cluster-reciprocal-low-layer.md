# Fixed-cluster reciprocal low-layer implementation plan

1. Generalize the reciprocal pointwise layer estimate to an arbitrary fixed
   cluster through the existing bucket-input interface.
2. Combine the global reciprocal-zero mass bound with a polynomial selected-
   height ceiling.
3. Prove normalized low-layer convergence under
   `sigma - beta + epsilon < 0`.
4. Add a public contract and axiom audit.
5. Compile serially and publish as an independent Draft PR based on Stack157.
6. In the next slice, replace the old low-layer input in the fixed-cluster
   Carlson boundary residual and propagate the improved threshold to the
   actual zero-package sign alternative.
