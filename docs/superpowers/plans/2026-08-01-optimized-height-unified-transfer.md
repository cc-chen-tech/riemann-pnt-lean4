# Optimized-Height Unified Transfer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Feed the certified Pintz optimizer and its actual remainder/envelope certificates into the existing monotone variable-boundary PNT transfer.

**Architecture:** Define sigma-only optimized height and running-boundary abbreviations. Assemble the existing generic upper/signed transfer from automatic sigma-only parameters and Stack119/120 certificates. Add a zero-free-envelope corollary that squeezes the relative PNT error to zero.

**Tech Stack:** Lean 4, existing Stack115–120 `ZeroDensityLayerBudget` modules.

## Global Constraints

- Create one implementation module and matching contract/audit files.
- Do not modify protected complementary-zero, Sharp, or VK-edge files.
- Retain main positive/negative witnesses as assumptions.
- Validate through one low-priority overlay process.

---

### Task 1: Sigma-only optimized objects

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualPintzCertifiedGoodHeightUnifiedTransfer.lean`

- [ ] Define the optimized height using the automatic positive exponent.
- [ ] Define its natural running visible-zero boundary anchored at the automatic `beta0`.

### Task 2: Unified upper and zero-free decay facades

**Files:**
- Modify: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualPintzCertifiedGoodHeightUnifiedTransfer.lean`

- [ ] Derive eventual polynomial height domination and sampled divergence.
- [ ] Assemble `actualMonotoneVariableBoundaryUnifiedUpperSignedOmega` with the Stack120 remainder certificate.
- [ ] Transfer a common candidate zero-free envelope to the optimizer.
- [ ] Derive running-boundary zero-free decay and squeeze the real relative PNT error to zero.

### Task 3: Contract, audit, and PR

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualPintzCertifiedGoodHeightUnifiedTransferContract.lean`
- Create: `Test/ZeroDensityLayerBudgetActualPintzCertifiedGoodHeightUnifiedTransferAxiomAudit.lean`

- [ ] Check the optimized objects and both facade theorems.
- [ ] Audit both facade theorems.
- [ ] Build all three targets and publish a draft PR based on Stack120.
