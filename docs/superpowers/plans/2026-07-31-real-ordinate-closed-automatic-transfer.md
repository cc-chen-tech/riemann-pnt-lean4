# Real-Ordinate-Closed Automatic Transfer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Eliminate the real-ordinate residual hypothesis by applying the automatic joint-parameter transfer to a canonically enlarged finite cluster.

**Architecture:** Compose the existing real-ordinate adjunction API with stack45. Preserve strip-cap and visible-cluster obligations on the enlarged cluster.

**Tech Stack:** Lean 4, finite zeta-zero clusters, existing automatic good-height transfer.

## Global Constraints

- Modify only `ZeroDensityLayerBudget*.lean` and task design/plan files.
- Do not modify `ZeroForcedOscillationComplementaryBound.lean`.
- Do not modify VK-edge or sharp-oscillation files.
- Do not claim the strip cap or visible-cluster witness is automatic.

---

### Task 1: Close the real-ordinate residual

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualAutomaticJointParameterRealOrdinateClosedTransfer.lean`

**Interfaces:**
- Consumes: `exists_automaticGoodHeight_jointParameterNaturalTargetTransfer`, `actualCarlsonAdjoinRealOrdinateZeros_conjugationStable`, and `realOrdinateNontrivialZerosOutsideClusterFinset_adjoin_eq_empty`.
- Produces: `exists_automaticGoodHeight_jointParameterRealOrdinateClosedTransfer`.

- [ ] **Step 1: Instantiate stack45 on the enlarged cluster**

- [ ] **Step 2: Prove conjugation invariance of the enlarged cluster**

- [ ] **Step 3: Discharge the real-ordinate condition from the empty residual**

- [ ] **Step 4: Return the transfer with only cap and main-witness inputs**

### Task 2: Focused validation

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualAutomaticJointParameterRealOrdinateClosedTransferContract.lean`
- Create: `Test/ZeroDensityLayerBudgetActualAutomaticJointParameterRealOrdinateClosedTransferAxiomAudit.lean`

**Interfaces:**
- Consumes: the new theorem.
- Produces: `#check` and `#print axioms` evidence.

- [ ] **Step 1: Compile the implementation directly**

- [ ] **Step 2: Compile the contract with a minimal `.olean`**

- [ ] **Step 3: Compile the axiom audit**

Expected axiom set: `propext`, `Classical.choice`, `Quot.sound`.

### Task 3: Publish stack46

**Files:**
- Stage only the two docs and three Lean files in this plan.

**Interfaces:**
- Consumes: focused validation evidence.
- Produces: a draft PR based on stack45.

- [ ] **Step 1: Commit docs separately**

- [ ] **Step 2: Commit code and audits**

- [ ] **Step 3: Push and open the stacked draft PR**

