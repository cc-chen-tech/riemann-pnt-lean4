# Global Optimal Actual Unified Transfer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Run the actual unified PNT transfer at `alpha = globalCeiling - eta`.

**Architecture:** Compose stack59's canonical optimizer and global ceiling with stack55's actual near-optimal transfer. Preserve the optimizer witness and exact exponent identity while leaving the visible-cluster lower witness explicit.

**Tech Stack:** Lean 4, actual zeta-zero finsets, selected good heights, globally optimized Carlson parameters.

## Global Constraints

- Modify only `ZeroDensityLayerBudget*.lean` and task design/plan files.
- Do not modify `ZeroForcedOscillationComplementaryBound.lean`.
- Do not modify VK-edge or sharp-oscillation files.
- Do not hide or manufacture the visible-cluster anti-cancellation witness.

---

### Task 1: Global actual transfer

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualGlobalOptimalUnifiedTransfer.lean`

**Interfaces:**
- Consumes: stack55 actual near-optimal transfer and stack59 global optimizer.
- Produces: `exists_globallyNearOptimalAutomaticGoodHeight_globalRealPartBoundNaturalTargetTransfer`.

- [ ] **Step 1: fix sigma to the canonical optimizer**

- [ ] **Step 2: derive the fixed-sigma eta gap**

- [ ] **Step 3: invoke the existing actual transfer**

- [ ] **Step 4: preserve the global alpha identity and optimizer witness**

### Task 2: Contract, audit, and publication

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualGlobalOptimalUnifiedTransferContract.lean`
- Create: `Test/ZeroDensityLayerBudgetActualGlobalOptimalUnifiedTransferAxiomAudit.lean`

**Interfaces:**
- Consumes: the global actual transfer.
- Produces: focused declaration and axiom evidence.

- [ ] **Step 1: compile implementation and contract directly**

- [ ] **Step 2: run the focused axiom audit**

- [ ] **Step 3: commit docs and code separately**

- [ ] **Step 4: push and create a draft PR based on stack59**
