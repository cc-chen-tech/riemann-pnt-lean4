# Sigma-Only Running-Boundary Transfer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construct `alpha`, `epsilon`, and `beta0` from `sigma` and specialize the canonical running-boundary unified transfer.

**Architecture:** Explicit arithmetic definitions feed a single certificate theorem. The final facade destructures that certificate and applies Stack 115.

**Tech Stack:** Lean 4, linear arithmetic, repository finite real-zero bottleneck and running-boundary modules.

## Global Constraints

- Modify only `ZeroDensityLayerBudget*`, matching contract/audit files, and task documents.
- Preserve exact moving upper and signed lower scales.
- Keep both signed main witnesses explicit.
- Do not touch protected complementary-bound or Sharp/VK-edge files.
- Do not claim unconditional Omega or RH.

---

### Task 1: Sigma-only numerical certificate

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetVariableBoundarySigmaOnlyUnifiedUpperSignedOmega.lean`

**Interfaces:**
- Consumes: `actualDynamicBoundaryBalancedGoodHeightExponent`, `realOrdinatePNTZeroBottleneck`, and its bounds.
- Produces: epsilon, joint bottleneck, beta0, and one certificate theorem containing every Stack 115 numerical hypothesis.

- [ ] Define the three automatic numerical quantities.
- [ ] Prove the joint bottleneck is nonnegative and below one.
- [ ] Prove the combined Stack 115 parameter certificate.

### Task 2: Sigma-only unified facade

**Files:**
- Modify: `PrimeNumberTheorem/ZeroDensityLayerBudgetVariableBoundarySigmaOnlyUnifiedUpperSignedOmega.lean`
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetVariableBoundarySigmaOnlyUnifiedUpperSignedOmegaContract.lean`
- Create: `Test/ZeroDensityLayerBudgetVariableBoundarySigmaOnlyUnifiedUpperSignedOmegaAxiomAudit.lean`

**Interfaces:**
- Consumes: Task 1 certificate and Stack 115.
- Produces: `actualSigmaOnlyNaturalRunningMaximumBoundaryUnifiedUpperSignedOmega`.

- [ ] State the final theorem with only `sigma`, transfer constants, and signed main witnesses.
- [ ] Apply Stack 115 using the automatic certificate.
- [ ] Add contract and axiom audit targets.
- [ ] Compile all three targets sequentially.
- [ ] Commit, push, and open a draft PR based on Stack 115.
