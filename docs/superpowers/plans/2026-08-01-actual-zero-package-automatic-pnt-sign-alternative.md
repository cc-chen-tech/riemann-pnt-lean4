# Actual Zero-Package Automatic PNT Sign Alternative Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Derive one unnormalized PNT oscillation sign from a nonempty attained right-edge seed without external package-energy or boundary-threshold inputs.

**Architecture:** Invoke stack98 to construct the actual package budget, then feed its package height, smoothing window, positive energy, cap, and boundary inequality into stack95.

**Tech Stack:** Lean 4, actual zeta package energy, Carlson boundary capture, explicit-formula PNT witness transfer.

## Global Constraints

- Add only the automatic PNT sign-alternative facade, contract, audit, and docs.
- Keep global right-edge cap and real-ordinate strictness explicit.
- Claim only `Omega+ OR Omega-` at `x^beta` scale.

---

### Task 1: Automatic facade

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualZeroPackageAutomaticPNTSignAlternative.lean`

**Interfaces:**
- Consumes: stack98 automatic package budget and stack95 unnormalized sign alternative.
- Produces: `exists_targetLineSeed_actualCarlsonPNTUnnormalizedSignAlternative_automatic`.

- [ ] **Step 1:** invoke stack98 with the seed and fixed sampling coefficient.
- [ ] **Step 2:** pass the generated package budget to stack95.
- [ ] **Step 3:** return every intermediate certificate and the final sign disjunction.

### Task 2: Contract and audit

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualZeroPackageAutomaticPNTSignAlternativeContract.lean`
- Create: `Test/ZeroDensityLayerBudgetActualZeroPackageAutomaticPNTSignAlternativeAxiomAudit.lean`

**Interfaces:**
- Consumes: the Task 1 theorem.
- Produces: a public type check and axiom report.

- [ ] **Step 1:** compile the facade with the local olean overlay.
- [ ] **Step 2:** compile contract and audit serially.
- [ ] **Step 3:** inspect the final type and axiom allowlist.
- [ ] **Step 4:** commit docs and Lean code separately and publish a draft PR based on stack98.
