# Actual explicit-formula unified target transfer plan

## Files

- `PrimeNumberTheorem/ZeroDensityLayerBudgetActualExplicitFormulaUnifiedTargetTransfer.lean`
- `PrimeNumberTheorem/ZeroDensityLayerBudgetActualExplicitFormulaUnifiedTargetTransferContract.lean`
- `Test/ZeroDensityLayerBudgetActualExplicitFormulaUnifiedTargetTransferAxiomAudit.lean`

## Theorem chain

1. Obtain eventual positivity of `targetZeroPowerAmplitude beta`.
2. Apply stack39 to the complete outside-cluster tail.
3. Transfer that estimate to the signed complement using its norm bound.
4. Apply the existing closed real-axis target-decay theorem.
5. Apply the polynomial explicit-formula remainder certificate.
6. Instantiate the exact visible-cluster explicit-formula decomposition.
7. Apply `unified_parametricPNTUpper_targetAmplitudeLower`.
8. Export intermediate complement-negligibility and final unified theorems.

## Validation

Compile implementation, contract, and audit directly without regenerating
the repository IR cache.

## PR boundary

The PR contains the concrete unified transfer only.  Automatic construction
of the visible-cluster witness from a finite rightmost zero cluster belongs
in the next mathematical stack.
