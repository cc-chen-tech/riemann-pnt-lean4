# Near-Optimal Actual Unified Transfer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Apply the actual unified PNT transfer at `alpha = optimized ceiling - eta`.

**Architecture:** Consume stack54's complete parameter tuple, derive the selected-height cap from the global zero bound, close real ordinates by cluster adjunction, and invoke the existing automatic good-height transfer.

**Tech Stack:** Lean 4, actual zeta-zero finsets, optimized two-height parameters, explicit-formula natural-point transfer.

## Global Constraints

- Modify only `ZeroDensityLayerBudget*.lean` and task design/plan files.
- Do not modify `ZeroForcedOscillationComplementaryBound.lean`.
- Do not modify VK-edge or sharp-oscillation files.
- Preserve the exact identity `alpha = ceiling - eta`.

---

### Task 1: Near-optimal actual transfer

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualNearOptimalUnifiedTransfer.lean`

**Interfaces:**
- Consumes: `exists_jointTwoHeightNearOptimalTruncationParameters` and `unified_automaticGoodHeight_twoHeight_naturalTargetTransfer`.
- Produces: `exists_nearOptimalAutomaticGoodHeight_globalRealPartBoundNaturalTargetTransfer`.

- [ ] **Step 1: Obtain the near-optimal full parameter tuple**

- [ ] **Step 2: Prove conjugation invariance and empty real-ordinate residual**

- [ ] **Step 3: Derive the selected-height cap from `rho.re <= theta < tau`**

- [ ] **Step 4: Invoke the actual unified transfer**

### Task 2: Contract, audit, and publication

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualNearOptimalUnifiedTransferContract.lean`
- Create: `Test/ZeroDensityLayerBudgetActualNearOptimalUnifiedTransferAxiomAudit.lean`

**Interfaces:**
- Consumes: the new theorem.
- Produces: focused API and axiom evidence.

- [ ] **Step 1: Add contract and audit**

- [ ] **Step 2: Compile directly with minimal `.olean` files**

- [ ] **Step 3: Commit docs and code separately**

- [ ] **Step 4: Open a draft PR based on stack54**

