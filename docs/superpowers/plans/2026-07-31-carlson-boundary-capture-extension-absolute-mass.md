# Carlson boundary capture extension absolute-mass plan

1. Add a non-strict absolute-mass truncated split using the existing
   `finite_actualHighPositiveZeroKernelSum_le_CarlsonTail_of_le` theorem.
2. Specialize the canonical two-strip low-layer decay to natural points and
   prove the positive outside absolute-mass boundary estimate.
3. Upgrade to full outside absolute mass and arbitrary moving finite
   extensions.
4. Combine with `exists_actualCarlsonFiniteSeedGapTransferCluster` to obtain a
   strict prescribed extension budget.
5. Expose the chain through a contract and audit every declaration's axioms.

