# Global Real-Part Bound Unified Transfer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Derive the automatic selected-height strip cap and unified transfer from a global positive nontrivial-zero real-part bound.

**Architecture:** Compose stack47's prescribed-cap arithmetic, the positive-zero membership API, real-ordinate cluster adjunction, and stack43's automatic good-height unified transfer.

**Tech Stack:** Lean 4, actual zeta-zero finsets, Carlson two-height estimates, explicit-formula natural-point transfer.

## Global Constraints

- Modify only `ZeroDensityLayerBudget*.lean` and task design/plan files.
- Do not modify `ZeroForcedOscillationComplementaryBound.lean`.
- Do not modify VK-edge or sharp-oscillation files.
- Keep the global real-part bound and visible-cluster witness explicit.

---

### Task 1: Global-bound transfer theorem

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualGlobalRealPartBoundUnifiedTransfer.lean`

**Interfaces:**
- Consumes: `exists_jointTwoHeightTargetAmplitudeParameters_above_cap`, `mem_positiveNontrivialZerosOutsideClusterFinset`, real-ordinate adjunction, and `unified_automaticGoodHeight_twoHeight_naturalTargetTransfer`.
- Produces: `exists_automaticGoodHeight_globalRealPartBoundNaturalTargetTransfer`.

- [ ] **Step 1: Select prescribed-cap parameters**

- [ ] **Step 2: Enlarge the cluster and prove conjugation invariance**

- [ ] **Step 3: Derive the strip cap from the global zero bound**

- [ ] **Step 4: Close the real-ordinate residual by emptiness**

- [ ] **Step 5: Apply the unified transfer with only the main witness left**

### Task 2: Contract and axiom audit

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualGlobalRealPartBoundUnifiedTransferContract.lean`
- Create: `Test/ZeroDensityLayerBudgetActualGlobalRealPartBoundUnifiedTransferAxiomAudit.lean`

**Interfaces:**
- Consumes: the new theorem.
- Produces: focused API and axiom evidence.

- [ ] **Step 1: Add the contract**

- [ ] **Step 2: Add the axiom audit**

- [ ] **Step 3: Compile all three files directly**

Expected axiom set: `propext`, `Classical.choice`, `Quot.sound`.

### Task 3: Publish stack48

**Files:**
- Stage only the two docs and three Lean files in this plan.

**Interfaces:**
- Consumes: focused validation evidence.
- Produces: a draft PR based on stack47.

- [ ] **Step 1: Commit docs separately**

- [ ] **Step 2: Commit code and audits**

- [ ] **Step 3: Push and open the stacked draft PR**

