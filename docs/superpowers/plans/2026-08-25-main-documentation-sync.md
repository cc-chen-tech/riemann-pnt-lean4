# Main Documentation Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reconcile every public status document with `origin/main` after PRs #474 and #478--#482 while preserving the distinction between proved Lean theorems, externally verified Zeta23 inputs, conditional theorem interfaces, and MWKF research reductions.

**Architecture:** Treat the recursive Prop inventory as the source of truth for declaration counts and generated status data. Keep `README.md` Chinese-first and public-facing, keep `PUBLISHING.md` conservative, and use the inventory/index/atlas files for reviewer-level detail. Do not describe open PRs #483 or #484 as merged `main` results.

**Tech Stack:** Markdown, JSON generated by Python 3, embedded HTML/JavaScript proof atlas, Lean 4 declaration inventory scripts.

**Spec:** `README.md`, `PUBLISHING.md`, `scripts/target_inventory.py`, and the declarations currently reachable from `origin/main` at `42f9f8fe49a0ba5a6b1935b3c2181360d4597dc0`.

## Global Constraints

- Keep the README Chinese-first with a plain-language opening and supporting English summary.
- State that Selberg/Conrey are closed in this repository's kernel through the independently machine-checked Anthropic Zeta23 Theorem B; do not present that analytic proof as this repository's own work.
- State that the native Selberg mollifier/MWKF route remains open at the coupled operator, centered Möbius--Farey trilinear, and transform-tail inputs.
- Keep PR #474's `14/17` and `2/3` endpoints explicitly conditional on their forcing/gate suppliers.
- Do not add #483 or #484 to the verified `main` theorem surface.
- Make no Lean source changes.

---

### Task 1: Synchronize generated target status

**Files:**
- Modify: `scripts/update-target-status.py`
- Regenerate: `docs/current-target-status.json`

**Interfaces:**
- Consumes: `scripts/target_inventory.py` classifications on current `main`.
- Produces: 14 mathematical targets, 5 route interfaces, 196 reusable predicates, 0 unclassified declarations, plus a Selberg/Conrey status that records Zeta23 closure and the still-open native route.

- [ ] **Step 1: Update the quantitative critical-line chain summary**

Replace the stale “Selberg and Conrey counts remain open” text with language that records kernel closure through Zeta23 and identifies the native mollifier proof as independent open work.

- [ ] **Step 2: Regenerate the JSON**

Run: `python3 scripts/update-target-status.py`

Expected: timestamp `2026-08-25`, totals `14 / 5 / 196 / 0`, and no stale “Selberg and Conrey counts remain open” phrase.

- [ ] **Step 3: Validate inventory consistency**

Run: `python3 scripts/check-targets-consistent.py`

Expected: `target inventory consistent: 14 mathematical targets, 5 route interfaces (0 body=True), 196 reusable predicates`.

### Task 2: Reconcile README and publishing claims

**Files:**
- Modify: `README.md`
- Modify: `PUBLISHING.md`

**Interfaces:**
- Consumes: generated counts and the merged research records under `docs/research/2026-08-24-*`.
- Produces: one consistent public narrative and a conservative publication boundary.

- [ ] **Step 1: Correct the README proof architecture and non-results sections**

Show the Zeta23-to-Selberg/Conrey closure separately from the native mollifier route, and remove Selberg/Conrey from unconditional “open target” wording.

- [ ] **Step 2: Add the merged MWKF research ledger**

Link the LCM main-term audit, exact off-diagonal audit, published-coverage map, centered trilinear reduction, and global coupled audit. Label all five as research reductions/scripts rather than new Lean theorem-level results.

- [ ] **Step 3: Stabilize the active-research section**

Describe route status rather than volatile local/remote branch labels. Treat open PRs as live GitHub state, not part of `main`.

- [ ] **Step 4: Extend the focused verification command**

Add `Test.Zeta23SelbergBridgeContract`, `Test.Zeta23SelbergBridgeAxiomAudit`, and representative PR #474 contract/axiom-audit targets.

- [ ] **Step 5: Correct PUBLISHING counts and trust language**

Update 12 to 14 and 48 to 196, explain that two target-shaped declarations are discharged through Zeta23, and distinguish native analytic reproof from kernel closure.

### Task 3: Reconcile reviewer-facing inventories and atlas

**Files:**
- Modify: `docs/formal-theorem-inventory.md`
- Modify: `docs/missing-chains-index.md`
- Modify: `docs/target-statements-and-chains.md`
- Modify: `docs/assets/riemann-proof-atlas.html`

**Interfaces:**
- Consumes: generated status JSON and README/PUBLISHING terminology.
- Produces: reviewer documents with the same counts and the same four trust levels.

- [ ] **Step 1: Update formal theorem inventory counts and target namespaces**

Record 14 mathematical targets and add the two `FiniteSpectrumGap` targets. Reword older Selberg/Conrey passages to distinguish native proof status from Zeta23 closure.

- [ ] **Step 2: Update the target statement and missing-chain indexes**

Record 14 targets and 196 reusable predicates. Add the two `FiniteSpectrumGap` targets, mark the two Zeta23-discharged target declarations in the mapping, and add the MWKF research obstruction as an additional non-Prop research gap.

- [ ] **Step 3: Update proof atlas progress text**

Add the Zeta23 closure and the merged MWKF audit boundary without depicting `14/17`, `2/3`, or the `N=T^3` long mollifier asymptotic as proved.

### Task 4: Verify and review the documentation slice

**Files:**
- Verify all modified files.

**Interfaces:**
- Consumes: Tasks 1--3.
- Produces: clean docs-only diff with generated data and prose in agreement.

- [ ] **Step 1: Check generated status and stale phrases**

Run the status generator a second time and require `git diff --exit-code docs/current-target-status.json` after recording the first generated result. Search for stale `12`/`48` counts and unqualified Selberg/Conrey-open claims.

- [ ] **Step 2: Check Markdown links and whitespace**

Run the repository-local README link checker script embedded in the task audit and `git diff --check`.

- [ ] **Step 3: Run repository status checks**

Run:

```bash
python3 scripts/check-targets-consistent.py
python3 scripts/check-chain-gaps.py
python3 scripts/list-prop-targets.py
python3 scripts/check_axiom_allowlist.py
python3 -m pytest -q
```

Expected: 14 mathematical targets, 5 route interfaces, 196 reusable predicates, 0 unclassified declarations, four chain buckets, standard axioms only, and all Python tests passing.

- [ ] **Step 4: Review the final diff**

Confirm only documentation, the status generator, and generated status JSON changed; confirm `git status --short` contains no unrelated user files.
