# Automatic Joint-Parameter Unified Transfer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Hide all numerical margin witnesses behind one existential automatic good-height unified transfer theorem.

**Architecture:** Compose stack44's joint parameter theorem with stack43's automatic good-height unified transfer. Expose only the three parameters referenced by the remaining analytic obligations.

**Tech Stack:** Lean 4, Mathlib filters, existing Pintz-Carlson explicit-formula transfer modules.

## Global Constraints

- Modify only `ZeroDensityLayerBudget*.lean` and task design/plan files.
- Do not modify `ZeroForcedOscillationComplementaryBound.lean`.
- Do not modify VK-edge or sharp-oscillation files.
- Preserve the distinction between automatic numerical feasibility and open analytic hypotheses.

---

### Task 1: Hidden-margin transfer theorem

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualAutomaticJointParameterUnifiedTransfer.lean`

**Interfaces:**
- Consumes: `exists_jointTwoHeightTargetAmplitudeParameters` and `unified_automaticGoodHeight_twoHeight_naturalTargetTransfer`.
- Produces: `exists_automaticGoodHeight_jointParameterNaturalTargetTransfer`.

- [ ] **Step 1: Eliminate hidden numerical witnesses**

Destructure the seven witnesses from stack44 and expose only `sigma`, `tau`,
and `alpha`.

- [ ] **Step 2: Preserve exact analytic obligations**

Quantify over the good-height selection and accept only the strip cap,
real-ordinate bound, and visible-cluster witness used by stack43.

- [ ] **Step 3: Apply the unified transfer**

Pass the hidden balanced cuts, margins, and positivity facts to stack43 and
return its paired upper/lower conclusion.

- [ ] **Step 4: Compile directly**

Run:

```bash
lake env lean PrimeNumberTheorem/ZeroDensityLayerBudgetActualAutomaticJointParameterUnifiedTransfer.lean
```

Expected: exit code 0.

### Task 2: Contract and axiom audit

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualAutomaticJointParameterUnifiedTransferContract.lean`
- Create: `Test/ZeroDensityLayerBudgetActualAutomaticJointParameterUnifiedTransferAxiomAudit.lean`

**Interfaces:**
- Consumes: `exists_automaticGoodHeight_jointParameterNaturalTargetTransfer`.
- Produces: focused API and axiom evidence.

- [ ] **Step 1: Add `#check` contract**

- [ ] **Step 2: Add `#print axioms` audit**

- [ ] **Step 3: Compile implementation, contract, and audit directly**

Expected axiom set: `propext`, `Classical.choice`, `Quot.sound`.

### Task 3: Publish stack45

**Files:**
- Stage only the two docs and three Lean files in this plan.

**Interfaces:**
- Consumes: focused validation evidence.
- Produces: a draft PR based on stack44.

- [ ] **Step 1: Commit docs separately**

- [ ] **Step 2: Commit Lean implementation and audits**

- [ ] **Step 3: Push and create a draft PR**

Use base
`research/pintz-carlson-stack-44-joint-two-height-parameter-feasibility`.
