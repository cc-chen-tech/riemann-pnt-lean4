# Improved Cap Automatic Actual Transfer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Automatically construct and run the globally optimized actual PNT transfer from the improved cap hypothesis.

**Architecture:** Use stack62 to obtain a positive global contour gap and stack60 to realize an actual transfer at a strict loss inside that gap. Return the loss and exact height identity for auditability.

**Tech Stack:** Lean 4, improved global cap threshold, actual selected-good-height PNT transfer.

## Global Constraints

- Modify only `ZeroDensityLayerBudget*.lean` and task design/plan files.
- Do not modify `ZeroForcedOscillationComplementaryBound.lean`.
- Do not modify VK-edge or sharp-oscillation files.
- Keep the visible-cluster witness explicit.

---

### Task 1: Automatic improved-cap actual transfer

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualImprovedCapAutomaticTransfer.lean`

**Interfaces:**
- Consumes: stack62 positive-gap theorem and stack60 global actual transfer.
- Produces: `exists_improvedCapAutomaticGoodHeight_globalRealPartBoundNaturalTargetTransfer`.

- [ ] **Step 1: obtain a positive eta inside the global contour gap**

- [ ] **Step 2: invoke the globally near-optimal actual transfer**

- [ ] **Step 3: return eta, optimizer, endpoint, exact alpha, and transfer**

### Task 2: Contract, audit, and publication

**Files:**
- Create: `PrimeNumberTheorem/ZeroDensityLayerBudgetActualImprovedCapAutomaticTransferContract.lean`
- Create: `Test/ZeroDensityLayerBudgetActualImprovedCapAutomaticTransferAxiomAudit.lean`

**Interfaces:**
- Consumes: the improved-cap actual theorem.
- Produces: focused declaration and axiom evidence.

- [ ] **Step 1: compile implementation and contract directly**

- [ ] **Step 2: run the focused axiom audit**

- [ ] **Step 3: commit docs and code separately**

- [ ] **Step 4: push and create a draft PR based on stack62**
