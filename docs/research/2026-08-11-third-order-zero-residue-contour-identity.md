# Explicit zero residue in the scaled contour identity

This slice transports the zero-pole coefficient identified in the boundary
rectangle theorem to the scaled right-edge contour identity.

The resulting theorem states that the actual scaled vertical integral equals
the finite corrected residue sum minus the genuine third-order contour
remainder, while retaining the exact identity

```text
residue 0 = iteratedDeriv 2 (thirdOrderZeroCore x) 0 / 2.
```

No analytic assumptions, contour constants, pole classifications, or remainder
definitions are changed. This is the direct prerequisite for exposing the same
coefficient in the `secondSmoothedChebyshevPsi` explicit formula.

No Omega theorem, RH consequence, or new zero-free region is claimed.
