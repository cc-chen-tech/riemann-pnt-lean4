# Actual zero-package automatic natural remainder

The uniform natural-point good-height selector automatically supplies:

```text
selectedUniformGoodHeight(alpha, selection, x) -> infinity
```

and the natural-point explicit-formula contour remainder certificate whenever

```text
beta > 0,
0 < alpha <= 1,
1 - beta < alpha.
```

Consequently, the actual zero-package lower transfer no longer needs either
of those facts as external inputs.  If the equal-real-part package is
nonempty, the module also chooses a window `L > 0` with positive package
energy.

The remaining explicit hypothesis is the Carlson outside-cluster good-height
finite-strip certificate for that same package and selector.  Removing this
last hypothesis requires a genuine quantitative complement theorem, not an
interface rearrangement.
