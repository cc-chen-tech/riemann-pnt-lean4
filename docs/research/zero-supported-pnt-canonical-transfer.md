# Canonical zero-supported PNT transfer

This specialization fixes the surviving actual-PNT coefficient to `c / 2`
while retaining the selected extension's nontrivial-zero certificate.

Both unsigned and signed declarations preserve the scale

`(c / 2) * x ^ beta`.

The visible-cluster oscillation witness remains an implication premise. The
result is therefore a verified transfer interface, not an unconditional
Omega theorem.
